/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.mnstatic.com'
        ], // Agrega el dominio de la imagen
      },
};

export default nextConfig;
